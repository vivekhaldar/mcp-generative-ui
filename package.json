{
  "name": "mcp-gen-ui",
  "version": "0.1.4",
  "description": "MCP wrapper that auto-generates interactive UIs for tools using LLM",
  "license": "Apache-2.0",
  "type": "module",
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "main": "dist/index.js",
  "bin": {
    "mcp-gen-ui": "./dist/index.js"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --external:@anthropic-ai/sdk --external:openai --external:@ai-sdk/anthropic --external:@ai-sdk/openai --external:commander",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^1.0.0",
    "@ai-sdk/openai": "^1.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ai": "^4.0.0",
    "commander": "^12.0.0",
    "eventsource": "^2.0.2",
    "shell-quote": "^1.8.3"
  },
  "devDependencies": {
    "@types/eventsource": "^1.1.15",
    "@types/node": "^20.0.0",
    "@types/shell-quote": "^1.7.5",
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.3.0"
  }
}
